# ==========================================
# 🛡️ SignLanguageTalk 프로젝트 전용 설정
# ==========================================

# 1. 가상환경 (팀장님 설정 유지)
**/backend_env/

# 2. 보안 파일 (어느 폴더에 있든 .env로 시작하면 모두 차단)
**/.env*
**/*.env
! .env.example    # (팁) 설정 예시 파일은 올려두는 게 좋습니다.

# 3. AI 모델 가중치 (대용량 파일 업로드 방지)
ai_models/weights/*.pt
ai_models/weights/*.pth
ai_models/weights/*.h5

# 4. 데이터 파이프라인 및 로그
data_pipeline/logs/
*.log
.ipynb_checkpoints/

# 5. OS 및 기타 잡동사니
.DS_Store
Thumbs.db